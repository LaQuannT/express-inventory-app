extends layout

block content

  h1 #{title}

  form(method='POST' action='')
    div
      label(for='name') Brand:
      input(type='text', placeholder='Nike, Reebok ect.' name='name' value=(undefined===brand ? '': brand.name))

    div 
      label(for="description") Brand Description: 
      input(type='text' name="description" required='true' size=60 placeholder='Enter Brand summary here...' value=(undefined===brand ? '': brand.description)) 

    div
      button(type="submit") Submit

  if errors
   ul
    for error in errors
     li!= error.msg